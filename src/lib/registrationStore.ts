/**
 * @deprecated This file is dead code — a localStorage mock from an early
 * development phase. All data access is now via `src/lib/munApi.ts` + Supabase.
 *
 * Safe to delete once you have confirmed no import of this file exists in the
 * codebase (grep for "registrationStore").
 */
export {};
